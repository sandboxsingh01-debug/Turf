import { Trophy } from 'lucide-react'
import { Badge } from '@/components/ui/badge'
import { EmptyState } from '@/components/ui/state'
import { getSports } from '@/lib/admin/data'

export default async function AdminSportsPage() { const { data: sports } = await getSports(); return <div className="flex flex-col gap-8"><header><span className="text-[10px] font-black uppercase tracking-[0.25em] text-primary">Admin / Configuration</span><h1 className="mt-2 font-heading text-3xl font-black uppercase text-foreground">Sports</h1><p className="mt-2 text-sm text-muted-foreground">Enable or disable sports without deleting booking history.</p></header>{!sports?.length ? <EmptyState icon={Trophy} title="No sports configured" description="Configure sports in the database." /> : <div className="grid gap-[2px] bg-border sm:grid-cols-2">{sports.map((sport) => <div key={sport.id} className="flex flex-col gap-4 bg-card p-6"><div className="flex items-center justify-between"><div className="flex items-center gap-2"><Trophy className="size-4 text-primary" /><h2 className="font-heading font-bold uppercase text-foreground">{sport.name}</h2></div><Badge>{sport.active ? 'Active' : 'Inactive'}</Badge></div><p className="min-h-12 text-sm leading-6 text-muted-foreground">{sport.description || 'No description configured.'}</p><p className="text-xs text-muted-foreground">Safe archive controls can be added through an admin mutation; deletion is intentionally unavailable while bookings reference this sport.</p></div>)}</div>}</div> }
